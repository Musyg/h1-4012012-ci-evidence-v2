# Temporary security-test repository

Two GitHub Actions workflows that differ by exactly one token, the `--` separator
in a documented Vercel CLI invocation. Created to measure the behaviour for a
vendor security report, on a throwaway Vercel project owned by the reporter.

This repository is temporary and will be removed once the measurement is frozen.
